#include "holberton.h"
/**
 * _abs - computer the number absolute.
 *
 * @r: number absolute in negatives or positives.
 *
 * Return: 1 if is positive o negative  or 0 if not is number absolute.
 */
int _abs(int r)
{
if (r > 0)
{
return (r);
}
else
{
return (-r);
}
}
