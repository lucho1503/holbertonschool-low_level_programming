#include "holberton.h"
#include <stdio.h>

/**
 * main - entry point.
 * @argv: pointer to matriz.
 * @argc: unused attribute.
 *
 * Return: 0.
 */

int main(int __attribute__((__unused__)) argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
